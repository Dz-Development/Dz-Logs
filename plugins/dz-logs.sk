command /wb2:
    permission: {@permission}
    trigger:
        send a "post" request to "s" with the headers "Authorization: Token", and "Content-Type: application/json" and the body "{"""
        set {_resp} to the last http response
        #broadcast {_resp}
